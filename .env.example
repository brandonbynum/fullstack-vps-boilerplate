# ===========================================
# Full-Stack Boilerplate Environment Variables
# ===========================================
# Copy this file to .env and configure for your environment
# Generate secrets with: openssl rand -base64 64

# Environment
NODE_ENV=development

# ===========================================
# Database
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=fullstack

# ===========================================
# JWT Authentication
# ===========================================
# Generate with: openssl rand -base64 64
JWT_SECRET=your-jwt-secret-here-minimum-32-characters
JWT_REFRESH_SECRET=your-refresh-secret-here-minimum-32-characters

# Token expiration
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
MAGIC_LINK_EXPIRES_IN=5m

# ===========================================
# URLs
# ===========================================
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# ===========================================
# Email Service
# ===========================================
# Configure for production (optional in development)
# For production, use a service like Resend, SendGrid, or AWS SES
# Example for Resend: SMTP_HOST=smtp.resend.com, SMTP_USER=resend, SMTP_PASSWORD=re_xxxxx
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASSWORD=placeholder-smtp-password
EMAIL_FROM=noreply@example.com

# ===========================================
# Frontend (Vite)
# ===========================================
# For development, use the backend URL directly
# For production, leave empty to use relative paths (proxied through nginx)
VITE_API_URL=http://localhost:4000
# Use quotes for values with spaces
VITE_APP_NAME="Full-Stack App"

# ===========================================
# Production Only
# ===========================================
# DOMAIN=yourdomain.com
# For production deployment, update these values:
# - NODE_ENV=production
# - POSTGRES_PASSWORD=<generate with: openssl rand -base64 32>
# - JWT_SECRET=<generate with: openssl rand -base64 64>
# - JWT_REFRESH_SECRET=<generate with: openssl rand -base64 64>
# - FRONTEND_URL=http://your-server-ip:8080 (or https://yourdomain.com after SSL setup)
# - ALLOWED_ORIGINS=http://your-server-ip:8080,http://yourdomain.com,https://yourdomain.com
# - VITE_API_URL= (leave empty for production to use relative paths)
# - SMTP_* values with real email service credentials
