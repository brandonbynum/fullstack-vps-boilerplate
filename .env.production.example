# ===========================================
# Production Environment Variables
# ===========================================
# Copy this file to .env on your VPS and configure for production
# SECURITY: Never commit .env file to version control!

# Environment
NODE_ENV=production

# ===========================================
# Domain Configuration
# ===========================================
DOMAIN=yourdomain.com

# ===========================================
# Database
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=REPLACE_WITH_SECURE_PASSWORD
POSTGRES_DB=fullstack

# Generate secure password with:
# openssl rand -base64 32 | tr -d '\n'

# ===========================================
# JWT Authentication
# ===========================================
# CRITICAL: Generate strong secrets for production!
# Generate with: openssl rand -base64 64 | tr -d '\n'
JWT_SECRET=REPLACE_WITH_JWT_SECRET
JWT_REFRESH_SECRET=REPLACE_WITH_JWT_REFRESH_SECRET

# Token expiration
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
MAGIC_LINK_EXPIRES_IN=5m

# ===========================================
# URLs
# ===========================================
FRONTEND_URL=https://yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===========================================
# Email Service (Optional - configure when ready)
# ===========================================
# Recommended: Resend (https://resend.com) - Free tier: 3000 emails/month
# Alternative: SendGrid, Mailgun, Amazon SES

# For Resend:
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASSWORD=re_YourResendAPIKey
EMAIL_FROM=noreply@yourdomain.com

# ===========================================
# Frontend (Vite Build Variables)
# ===========================================
# These are used during frontend build
VITE_API_URL=/api
VITE_APP_NAME=My Full-Stack App

# ===========================================
# Production Setup Notes
# ===========================================
#
# 1. Generate Secrets:
#    JWT_SECRET=$(openssl rand -base64 64 | tr -d '\n')
#    JWT_REFRESH_SECRET=$(openssl rand -base64 64 | tr -d '\n')
#    DB_PASSWORD=$(openssl rand -base64 32 | tr -d '\n')
#
# 2. Replace Placeholders:
#    - DOMAIN: Your actual domain name
#    - JWT_SECRET: Generated secret
#    - JWT_REFRESH_SECRET: Generated secret
#    - POSTGRES_PASSWORD: Generated password
#    - SMTP credentials: Your email service credentials (if using email)
#
# 3. Set Correct Permissions:
#    chmod 600 .env
#    chown appuser:appuser .env
#
# 4. Verify Configuration:
#    grep "REPLACE" .env  # Should return nothing
#
